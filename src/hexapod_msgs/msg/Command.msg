# Type of command: e.g., "save", "delete", "reset"
string command_type

# Optional pose name (used for save/delete)
string pose_name

# Optional index (e.g. for saving at a specific index)
int32 index

# Optional pose (only used for save)
# your_package/LegPoses pose
